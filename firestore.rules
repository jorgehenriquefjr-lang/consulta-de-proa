rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Histórico de consultas: leitura pública, escrita somente pelas Cloud
    // Functions (Admin SDK ignora estas regras).
    match /consultas/{doc} {
      allow read: if true;
      allow write: if false;
    }
  }
}
